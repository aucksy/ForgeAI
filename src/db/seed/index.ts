/**
 * Demo-data seeder entry point. Replaced by the seed module with a full
 * 3-month realistic history (exercises, plans, sessions, meals, PRs, weight).
 */
export async function ensureSeeded(): Promise<void> {
  // stub — implemented by the seed module
}

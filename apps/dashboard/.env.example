# Optional overrides. If unset, the app falls back to the beta project's publishable
# key (RLS-guarded, safe to ship — the same key that ships in the mobile app.json).
VITE_SUPABASE_URL=https://hdxzqbdiokjklnypfisq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx

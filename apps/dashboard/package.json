{
  "name": "forgeai-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ForgeAI gym CRM — the owner's web app (Vite + React + TS). Local-first data adapter for demo/offline, Supabase adapter for production.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {
    "@forgeai/theme": "*",
    "@supabase/supabase-js": "^2.110.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^3.2.7"
  }
}

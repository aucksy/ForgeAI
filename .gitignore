# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders — iOS is not used; ANDROID is committed (built in CI),
# so we track its source but ignore build outputs, local config and ALL keystores.
/ios
android/build/
android/app/build/
android/.gradle/
android/.cxx/
android/.kotlin/
android/local.properties
android/app/release.keystore
*.keystore
